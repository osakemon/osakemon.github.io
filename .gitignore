_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_Store
.ruby-version
Gemfile.lock
vendor

# Private scratch space. Anything Jekyll does not recognise at the top level is
# copied verbatim into _site and served publicly, so keep working documents
# (resumes, drafts) both untracked here and excluded in _config.yml.
cv_work/
